<scriptlet data = {
    message: 'Welcome to {{projectName}}'
  };

  onMount(() => {
    console.log('{{projectName}} app mounted');
  });
</script>

<div class="app{{#if useTailwind}} min-h-screen flex flex-col{{/if}}">
  <Header />
  
  <main class="{{#if useTailwind}}flex-1 container mx-auto px-4 py-8{{else}}main-content{{/if}}">
    <div class="{{#if useTailwind}}text-center{{else}}welcome{{/if}}">
      <h1 class="{{#if useTailwind}}text-4xl font-bold text-gray-800 mb-4{{else}}title{{/if}}">
        {data.message}
      </h1>
      <p class="{{#if useTailwind}}text-lg text-gray-600 mb-8{{else}}subtitle{{/if}}">
        Built with create-js-stack and SvelteKit
      </p>
      
      <div class="{{#if useTailwind}}grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6{{else}}features{{/if}}">
        <div class="{{#if useTailwind}}bg-white rounded-lg shadow-md p-6{{else}}feature-card{{/if}}">
          <h3 class="{{#if useTailwind}}text-xl font-semibold mb-2{{else}}feature-title{{/if}}">⚡ Fast</h3>
          <p class="{{#if useTailwind}}text-gray-600{{else}}feature-description{{/if}}">
            Built with Vite for lightning-fast development
          </p>
        </div>
        
        <div class="{{#if useTailwind}}bg-white rounded-lg shadow-md p-6{{else}}feature-card{{/if}}">
          <h3 class="{{#if useTailwind}}text-xl font-semibold mb-2{{else}}feature-title{{/if}}">🚀 Modern</h3>
          <p class="{{#if useTailwind}}text-gray-600{{else}}feature-description{{/if}}">
            Latest Svelte features with TypeScript support
          </p>
        </div>
        
        <div class="{{#if useTailwind}}bg-white rounded-lg shadow-md p-6{{else}}feature-card{{/if}}">
          <h3 class="{{#if useTailwind}}text-xl font-semibold mb-2{{else}}feature-title{{/if}}">📦 Ready</h3>
          <p class="{{#if useTailwind}}text-gray-600{{else}}feature-description{{/if}}">
            Pre-configured with best practices and tools
          </p>
        </div>
      </div>
    </div>
  </main>
  
  <Footer />
</div>

{{#unless useTailwind}}
<style>
  :global(body) {
    margin: 0;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    background-color: #f5f5f5;
  }

  .app {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }

  .main-content {
    flex: 1;
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem 1rem;
    width: 100%;
  }

  .welcome {
    text-align: center;
  }

  .title {
    font-size: 2.5rem;
    font-weight: bold;
    color: #333;
    margin-bottom: 1rem;
  }

  .subtitle {
    font-size: 1.2rem;
    color: #666;
    margin-bottom: 2rem;
  }

  .features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
    max-width: 900px;
    margin: 0 auto;
  }

  .feature-card {
    background: white;
    border-radius: 8px;
    padding: 1.5rem;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    transition: transform 0.2s;
  }

  .feature-card:hover {
    transform: translateY(-2px);
  }

  .feature-title {
    font-size: 1.3rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: #333;
  }

  .feature-description {
    color: #666;
    line-height: 1.5;
  }
</style>
{{/unless}}
