{{#if typescript}}/** @type {import('jest').Config} */
module.exports = {
  testEnvironment: '{{#if frontend}}jsdom{{else}}node{{/if}}',
  roots: ['<rootDir>/src'],
  testMatch: [
    '**/__tests__/**/*.ts',
    '**/*.(test|spec).ts'
  ],
  transform: {
    '^.+\\.ts$': 'ts-jest',
    {{#if frontend.react}}
    '^.+\\.tsx$': 'ts-jest',
    {{/if}}
  },
  collectCoverageFrom: [
    'src/**/*.ts',
    {{#if frontend.react}}
    'src/**/*.tsx',
    {{/if}}
    '!src/**/*.d.ts',
  ],
  {{#if frontend}}
  setupFilesAfterEnv: ['<rootDir>/src/setupTests.ts'],
  moduleNameMapping: {
    '\\.(css|less|scss|sass)$': 'identity-obj-proxy',
  },
  {{/if}}
  {{#if backend}}
  testTimeout: 10000,
  {{/if}}
};
{{else}}
/** @type {import('jest').Config} */
module.exports = {
  testEnvironment: '{{#if frontend}}jsdom{{else}}node{{/if}}',
  roots: ['<rootDir>/src'],
  testMatch: [
    '**/__tests__/**/*.js',
    '**/*.(test|spec).js'
  ],
  collectCoverageFrom: [
    'src/**/*.js',
    '!src/**/*.d.js',
  ],
  {{#if frontend}}
  setupFilesAfterEnv: ['<rootDir>/src/setupTests.js'],
  moduleNameMapping: {
    '\\.(css|less|scss|sass)$': 'identity-obj-proxy',
  },
  {{/if}}
  {{#if backend}}
  testTimeout: 10000,
  {{/if}}
};
{{/if}}
