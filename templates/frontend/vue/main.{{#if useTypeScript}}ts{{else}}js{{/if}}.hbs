{{#if useTypeScript}}import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import router from './router'
{{#if useTailwind}}
import './style.css'
{{/if}}

const app = createApp(App)

app.use(createPinia())
app.use(router)

app.mount('#app')
{{else}}import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import router from './router'
{{#if useTailwind}}
import './style.css'
{{/if}}

const app = createApp(App)

app.use(createPinia())
app.use(router)

app.mount('#app')
{{/if}}
