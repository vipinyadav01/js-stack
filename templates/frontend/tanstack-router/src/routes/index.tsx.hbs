import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/')({
  component: Index,
})

function Index() {
  return (
    <div className="p-2 flex gap-2 min-h-screen flex-col items-center justify-center bg-gray-100 dark:bg-gray-900">
      <h1 className="text-4xl font-bold mb-4 text-gray-900 dark:text-white">
        Welcome to <span className="text-teal-500">TanStack Router</span>
      </h1>
      <p className="text-lg text-gray-600 dark:text-gray-300 mb-8">
        Edit <code className="font-mono bg-gray-200 dark:bg-gray-800 px-2 py-1 rounded">src/routes/index.tsx</code> to get started.
      </p>
    </div>
  )
}
