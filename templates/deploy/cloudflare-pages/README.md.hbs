# Cloudflare Pages Setup

1. Install Wrangler: `npm install -g wrangler`
2. Login: `wrangler login`
3. Deploy: `npm run build && wrangler pages deploy dist` (adjust `dist` to your build output folder)
