# {{projectName}}

A modern JavaScript full-stack project generated with [create-js-stack](https://github.com/vipinyadav01/js-stack).

## 🚀 Tech Stack

{{#if frontend.length}}
### Frontend
{{#each frontend}}
- **{{this}}** - {{#if (eq this 'react')}}React with Vite{{else if (eq this 'vue')}}Vue.js with Vite{{else if (eq this 'nextjs')}}Next.js{{else if (eq this 'nuxt')}}Nuxt.js{{else if (eq this 'svelte')}}Svelte{{else if (eq this 'angular')}}Angular{{else}}{{this}}{{/if}}
{{/each}}
{{/if}}

{{#if (eq backend 'none')}}
{{else}}
### Backend
- **{{backend}}** - {{#if (eq backend 'express')}}Express.js{{else if (eq backend 'fastify')}}Fastify{{else if (eq backend 'nestjs')}}NestJS{{else if (eq backend 'hono')}}Hono{{else if (eq backend 'trpc')}}tRPC{{else}}{{backend}}{{/if}}
{{/if}}

{{#if (eq database 'none')}}
{{else}}
### Database
- **{{database}}** - {{#if (eq database 'postgres')}}PostgreSQL{{else if (eq database 'mongodb')}}MongoDB{{else if (eq database 'sqlite')}}SQLite{{else if (eq database 'supabase')}}Supabase{{else}}{{database}}{{/if}}
{{#if (eq orm 'none')}}
{{else}}
- **{{orm}}** - {{#if (eq orm 'prisma')}}Prisma ORM{{else if (eq orm 'mongoose')}}Mongoose{{else if (eq orm 'sequelize')}}Sequelize{{else if (eq orm 'typeorm')}}TypeORM{{else if (eq orm 'drizzle')}}Drizzle{{else}}{{orm}}{{/if}}
{{/if}}
{{/if}}

{{#if (eq auth 'none')}}
{{else}}
### Authentication
- **{{auth}}** - {{#if (eq auth 'jwt')}}JSON Web Tokens{{else if (eq auth 'auth0')}}Auth0{{else if (eq auth 'passport')}}Passport.js{{else if (eq auth 'nextauth')}}NextAuth.js{{else if (eq auth 'supabase')}}Supabase Auth{{else if (eq auth 'lucia')}}Lucia{{else if (eq auth 'better-auth')}}Better Auth{{else}}{{auth}}{{/if}}
{{/if}}

{{#if addons.length}}
### Tools & Addons
{{#each addons}}
- **{{this}}** - {{#if (eq this 'typescript')}}TypeScript{{else if (eq this 'eslint')}}ESLint{{else if (eq this 'prettier')}}Prettier{{else if (eq this 'biome')}}Biome{{else if (eq this 'tailwind')}}Tailwind CSS{{else if (eq this 'docker')}}Docker{{else if (eq this 'testing')}}Testing{{else if (eq this 'pwa')}}PWA{{else if (eq this 'turborepo')}}Turborepo{{else}}{{this}}{{/if}}
{{/each}}
{{/if}}

## 📁 Project Structure

```
{{projectName}}/
{{#if (includes addons 'turborepo')}}
├── apps/
│   {{#if frontend.length}}
│   ├── web/              # Frontend application
│   {{/if}}
│   {{#unless (eq backend 'none')}}
│   └── server/           # Backend application
│   {{/unless}}
├── packages/             # Shared packages
│   ├── ui/              # Shared UI components
│   └── config/          # Shared configurations
├── configs/             # Tool configurations
│   ├── eslint/          # ESLint configs
│   ├── typescript/      # TypeScript configs
│   └── tailwind/        # Tailwind configs
{{else}}
{{#if frontend.length}}
├── frontend/            # Frontend application
{{/if}}
{{#unless (eq backend 'none')}}
├── backend/             # Backend application
{{/unless}}
{{#unless (eq database 'none')}}
├── database/            # Database configuration
{{/unless}}
{{/if}}
├── package.json         # Project dependencies
{{#if typescript}}
├── tsconfig.json        # TypeScript configuration
{{/if}}
{{#if (includes addons 'eslint')}}
├── eslint.config.js     # ESLint configuration
{{/if}}
{{#if (includes addons 'prettier')}}
├── .prettierrc          # Prettier configuration
{{/if}}
{{#if (includes addons 'biome')}}
├── biome.json           # Biome configuration
{{/if}}
{{#if (includes addons 'docker')}}
├── Dockerfile           # Docker configuration
├── docker-compose.yml   # Docker Compose
{{/if}}
└── README.md            # This file
```

## 🚀 Getting Started

### Prerequisites
- Node.js (v18 or higher)
- {{packageManager}}

### Installation
```bash
{{#if (eq packageManager 'npm')}}
npm install
{{else if (eq packageManager 'yarn')}}
yarn install
{{else if (eq packageManager 'pnpm')}}
pnpm install
{{else if (eq packageManager 'bun')}}
bun install
{{/if}}
```

### Development
```bash
{{#if (eq packageManager 'npm')}}
npm run dev
{{else if (eq packageManager 'yarn')}}
yarn dev
{{else if (eq packageManager 'pnpm')}}
pnpm dev
{{else if (eq packageManager 'bun')}}
bun dev
{{/if}}
```

### Build
```bash
{{#if (eq packageManager 'npm')}}
npm run build
{{else if (eq packageManager 'yarn')}}
yarn build
{{else if (eq packageManager 'pnpm')}}
pnpm build
{{else if (eq packageManager 'bun')}}
bun build
{{/if}}
```

### Test
```bash
{{#if (eq packageManager 'npm')}}
npm test
{{else if (eq packageManager 'yarn')}}
yarn test
{{else if (eq packageManager 'pnpm')}}
pnpm test
{{else if (eq packageManager 'bun')}}
bun test
{{/if}}
```

## 📚 Available Scripts

{{#if (eq packageManager 'npm')}}
- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm start` - Start production server
- `npm test` - Run tests
- `npm run lint` - Run linter
- `npm run format` - Format code
{{else if (eq packageManager 'yarn')}}
- `yarn dev` - Start development server
- `yarn build` - Build for production
- `yarn start` - Start production server
- `yarn test` - Run tests
- `yarn lint` - Run linter
- `yarn format` - Format code
{{else if (eq packageManager 'pnpm')}}
- `pnpm dev` - Start development server
- `pnpm build` - Build for production
- `pnpm start` - Start production server
- `pnpm test` - Run tests
- `pnpm lint` - Run linter
- `pnpm format` - Format code
{{else if (eq packageManager 'bun')}}
- `bun dev` - Start development server
- `bun build` - Build for production
- `bun start` - Start production server
- `bun test` - Run tests
- `bun lint` - Run linter
- `bun format` - Format code
{{/if}}

## 🔧 Configuration

{{#if typescript}}
### TypeScript
This project uses TypeScript for type safety. Configuration is in `tsconfig.json`.
{{/if}}

{{#if (includes addons 'eslint')}}
### ESLint
Code linting is configured with ESLint. Run `{{#if (eq packageManager 'npm')}}npm{{else if (eq packageManager 'yarn')}}yarn{{else if (eq packageManager 'pnpm')}}pnpm{{else if (eq packageManager 'bun')}}bun{{/if}} run lint` to check for issues.
{{/if}}

{{#if (includes addons 'prettier')}}
### Prettier
Code formatting is handled by Prettier. Run `{{#if (eq packageManager 'npm')}}npm{{else if (eq packageManager 'yarn')}}yarn{{else if (eq packageManager 'pnpm')}}pnpm{{else if (eq packageManager 'bun')}}bun{{/if}} run format` to format code.
{{/if}}

{{#if (includes addons 'biome')}}
### Biome
Code linting and formatting is handled by Biome. Run `{{#if (eq packageManager 'npm')}}npm{{else if (eq packageManager 'yarn')}}yarn{{else if (eq packageManager 'pnpm')}}pnpm{{else if (eq packageManager 'bun')}}bun{{/if}} run lint` to check for issues and `{{#if (eq packageManager 'npm')}}npm{{else if (eq packageManager 'yarn')}}yarn{{else if (eq packageManager 'pnpm')}}pnpm{{else if (eq packageManager 'bun')}}bun{{/if}} run format` to format code.
{{/if}}

## 🚀 Deployment

{{#if (eq deployment 'none')}}
This project is ready for deployment to any platform that supports Node.js.
{{else}}
### {{deployment}}
{{#if (eq deployment 'vercel')}}
This project is configured for Vercel deployment. Simply connect your repository to Vercel.
{{else if (eq deployment 'netlify')}}
This project is configured for Netlify deployment. Simply connect your repository to Netlify.
{{else if (eq deployment 'cloudflare')}}
This project is configured for Cloudflare Workers deployment.
{{else if (eq deployment 'aws')}}
This project is configured for AWS deployment.
{{else if (eq deployment 'railway')}}
This project is configured for Railway deployment.
{{else if (eq deployment 'render')}}
This project is configured for Render deployment.
{{/if}}
{{/if}}

## 📖 Learn More

- [create-js-stack Documentation](https://github.com/vipinyadav01/js-stack)
- [Node.js Documentation](https://nodejs.org/docs/)
{{#if (includes frontend 'react')}}
- [React Documentation](https://react.dev/)
{{/if}}
{{#if (includes frontend 'vue')}}
- [Vue.js Documentation](https://vuejs.org/)
{{/if}}
{{#if (includes frontend 'nextjs')}}
- [Next.js Documentation](https://nextjs.org/docs)
{{/if}}
{{#if (includes frontend 'nuxt')}}
- [Nuxt.js Documentation](https://nuxt.com/)
{{/if}}
{{#if (eq backend 'express')}}
- [Express.js Documentation](https://expressjs.com/)
{{/if}}
{{#if (eq backend 'fastify')}}
- [Fastify Documentation](https://www.fastify.io/)
{{/if}}
{{#if (eq database 'postgres')}}
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
{{/if}}
{{#if (eq database 'mongodb')}}
- [MongoDB Documentation](https://docs.mongodb.com/)
{{/if}}

## 📄 License

This project is licensed under the MIT License.

---

Generated with ❤️ by [create-js-stack](https://github.com/vipinyadav01/js-stack)
