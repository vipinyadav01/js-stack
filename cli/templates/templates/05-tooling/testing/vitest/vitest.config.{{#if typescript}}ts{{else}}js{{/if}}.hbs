node',
    globals: true,
    setupFiles: ['./src/setupTests.ts'],
    css: true,
  },
  {{#if frontend}}
  resolve: {
    alias: {
      '@': new URL('./src', import.meta.url).pathname,
    },
  },
  {{/if}}
});
{{else}}
import { defineConfig } from 'vite';
{{#if frontend.react}}
import react from '@vitejs/plugin-react';
{{/if}}
{{#if frontend.vue}}
import vue from '@vitejs/plugin-vue';
{{/if}}

export default defineConfig({
  plugins: [
    {{#if frontend.react}}react(),{{/if}}
    {{#if frontend.vue}}vue(),{{/if}}
  ],
  test: {
    environment: '{{#if frontend}}jsdom{{else}}node{{/if}}',
    globals: true,
    setupFiles: ['./src/setupTests.js'],
    css: true,
  },
  {{#if frontend}}
  resolve: {
    alias: {
      '@': new URL('./src', import.meta.url).pathname,
    },
  },
  {{/if}}
});
{{/if}}
